<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_view"
    android:layout_width="80dp"
    android:layout_height="80dp"
    android:layout_marginEnd="8dp"
    app:cardElevation="2dp"
    app:cardCornerRadius="16dp"
    app:strokeWidth="2dp"
    app:strokeColor="@color/ai_assistant_border"
    app:cardBackgroundColor="@color/ai_assistant_card_background_light"
    android:clickable="true"
    android:focusable="true"
    android:checkable="true"
    app:checkedIcon="@drawable/ic_check_circle"
    app:checkedIconTint="?attr/colorPrimary"
    app:checkedIconSize="16dp"
    app:checkedIconMargin="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="8dp"
        android:gravity="center">

        <!-- 头像图标 -->
        <ImageView
            android:id="@+id/profile_avatar"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginBottom="4dp"
            android:src="@drawable/ic_person"
            android:background="@drawable/circle_background"
            android:padding="6dp"
            app:tint="?attr/colorOnPrimary"
            android:backgroundTint="@color/elderly_primary" />

        <!-- 档案名称 -->
        <TextView
            android:id="@+id/profile_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="默认"
            android:textSize="11sp"
            android:textStyle="bold"
            android:textColor="@color/ai_assistant_text_primary"
            android:gravity="center"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- 档案描述 -->
        <TextView
            android:id="@+id/profile_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="9sp"
            android:textColor="@color/ai_assistant_text_secondary"
            android:gravity="center"
            android:maxLines="1"
            android:ellipsize="end"
            android:visibility="gone" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>