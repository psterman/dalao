<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="2dp">

    <!-- 用户消息 -->
    <LinearLayout
        android:id="@+id/user_message_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginBottom="4dp"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginStart="48dp"
            android:gravity="end">

            <com.google.android.material.card.MaterialCardView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                app:cardBackgroundColor="@color/material_chat_user_bubble"
                app:cardCornerRadius="18dp"
                app:cardElevation="1dp"
                app:strokeWidth="0dp">

                <TextView
                    android:id="@+id/user_message_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingHorizontal="16dp"
                    android:paddingVertical="12dp"
                    android:textColor="@color/material_chat_user_text"
                    android:textSize="16sp"
                    android:lineSpacingExtra="4dp"
                    android:fontFamily="sans-serif"
                    android:letterSpacing="0.01"
                    android:maxWidth="280dp" />

            </com.google.android.material.card.MaterialCardView>

            <TextView
                android:id="@+id/user_message_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:layout_marginTop="2dp"
                android:layout_marginEnd="8dp"
                android:textSize="12sp"
                android:textColor="@color/material_dialog_text_hint"
                android:fontFamily="sans-serif" />

        </LinearLayout>

    </LinearLayout>

    <!-- AI消息 -->
    <LinearLayout
        android:id="@+id/ai_message_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="start"
        android:layout_marginBottom="4dp"
        android:visibility="gone">

        <!-- AI头像 -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="12dp"
            android:layout_marginTop="4dp"
            app:cardBackgroundColor="@color/material_dialog_surface_variant"
            app:cardCornerRadius="20dp"
            app:cardElevation="0dp"
            app:strokeWidth="1dp"
            app:strokeColor="@color/material_dialog_border">

            <ImageView
                android:id="@+id/ai_avatar"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_smart_toy"
                android:tint="@color/material_wechat_primary" />

        </com.google.android.material.card.MaterialCardView>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginEnd="48dp">

            <!-- AI名称 -->
            <TextView
                android:id="@+id/ai_name_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="4dp"
                android:text="AI助手"
                android:textSize="14sp"
                android:textColor="@color/material_dialog_text_secondary"
                android:fontFamily="sans-serif-medium" />

            <com.google.android.material.card.MaterialCardView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:cardBackgroundColor="@color/material_chat_ai_bubble"
                app:cardCornerRadius="18dp"
                app:cardElevation="1dp"
                app:strokeWidth="1dp"
                app:strokeColor="@color/material_dialog_border">

                <TextView
                    android:id="@+id/ai_message_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingHorizontal="16dp"
                    android:paddingVertical="12dp"
                    android:textColor="@color/material_chat_ai_text"
                    android:textSize="16sp"
                    android:lineSpacingExtra="4dp"
                    android:fontFamily="sans-serif"
                    android:letterSpacing="0.01"
                    android:maxWidth="280dp" />

            </com.google.android.material.card.MaterialCardView>

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="2dp"
                android:layout_marginStart="8dp">

                <TextView
                    android:id="@+id/ai_message_time"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="12sp"
                    android:textColor="@color/material_dialog_text_hint"
                    android:fontFamily="sans-serif" />

                <!-- 操作按钮 -->
                <ImageButton
                    android:id="@+id/btn_copy_message"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_marginStart="8dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_copy"
                    android:tint="@color/material_dialog_text_hint"
                    android:contentDescription="复制消息"
                    android:padding="4dp" />

                <ImageButton
                    android:id="@+id/btn_regenerate"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_marginStart="8dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_refresh"
                    android:tint="@color/material_dialog_text_hint"
                    android:contentDescription="重新生成"
                    android:padding="4dp" />

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</LinearLayout>
